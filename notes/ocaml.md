OCaml
=====
Install OCaml:
```
$ sudo pacman -S ocaml
```

Then to get `utop`, install opam:

```
$ sudo pacman -S opam
```

Then run

```
$ install opam ; opam init ; opam update ; opam install utop
```

